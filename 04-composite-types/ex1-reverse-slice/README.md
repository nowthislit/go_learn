# 练习1：切片反转

## 目标
- 掌握切片的创建和复制
- 理解切片是引用类型
- 学会不修改原切片创建新切片

## 运行
```bash
cd 04-composite-types/ex1-reverse-slice
go run main.go
```

## 关键步骤
1. 使用 `make([]int, len(s))` 创建等长新切片
2. 从原切片末尾开始遍历
3. 将元素按相反顺序放入新切片

## 注意
如果直接修改原切片反转，会更简单（双指针交换），
但题目要求创建新切片，需要额外内存。
